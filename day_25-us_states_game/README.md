# Day 25 - Working with CSV Data and the Pandas Library
## Overview
Pandas is a powerful python library for data analysis

The two primary data structure of pandas is **Series**(1-dimensional) and **Dataframe** (2-dimensional)

Series - Equivalent to a list
Dataframes - The whole 

## How to use
``` import pandas ```

### Filtering data
``` py
print(data[data["temp"] == data["temp"].max()])

```